import 'package:flutter/material.dart';
class TabletView extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Tablet",style: TextStyle(fontSize: 55),),
      ),
    );
  }
}